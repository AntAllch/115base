---
layout: base.njk 
title: 115base | Home
pageClass: lego
---
{# 
<ul>
  {% for post in collections.featuredPosts %}
    <li>
      <h2>{{ post.data.title }}</h2>
      <p>{{ post.data.description }}</p>
    </li>
  {% endfor %}
</ul> #}

<main class="hero">

  <h1 class="hero-title">Select a Map</h1>

  <div class="game-blocks">

    <a class="block-link" {% if page.fileSlug === 'libertyfalls' %}aria-current="page"{% endif %} href="/libertyfalls">
      <div class="game-block" id="liberty-falls">
        <p class="block-title">Liberty Falls</p>
      </div>
    </a>

    <a class="block-link" {% if page.fileSlug === 'terminus' %}aria-current="page"{% endif %} href="/terminus">
      <div class="game-block" id="terminus">
        <p class="block-title">Terminus</p>
      </div>
    </a>

  </div>

</main>